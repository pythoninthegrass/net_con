# Example environment variables for Taskfile
# Copy this file to .env and customize for your environment

# Docker image configuration
IMAGE_NAME=net-con
IMAGE_TAG=latest

# Registry configuration (required for push/CI tasks)
# Examples:
# REGISTRY_PREFIX=docker.io/yourusername/
# REGISTRY_PREFIX=gcr.io/your-project-id/
# REGISTRY_PREFIX=your-registry.com/your-namespace/
REGISTRY_PREFIX=

# Kubernetes context (optional - uses current context if not set)
# KUBECONFIG=/path/to/your/kubeconfig
# KUBECTL_CONTEXT=your-cluster-context

# Taskfile Env Precedence
# * Manipulate venv path
# * https://taskfile.dev/docs/experiments/env-precedence
TASK_X_ENV_PRECEDENCE=1
